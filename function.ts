

function stringToCharCode (str : string){

    const newStr = str.split("");
    return newStr;
}
function sendEmail(to: string, subject: string, message: string): void {
    // implementação da lógica de envio de e-mails
  }
  
  function validateInput(inputData: any): boolean {
    return false;
    // implementação da lógica de validação
  }
  function encryptData(data: string): string {
    // implementação da lógica de criptografia

    return data;
  }
//   async function getUserById(userId: number): Promise<User> {
//     // estabelecer a conexão com o banco de dados e executar consulta SQL

//     return userId;

 
        